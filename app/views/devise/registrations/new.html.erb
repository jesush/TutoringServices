<h2>User Sign up</h2>
<h3 class="subheader">Sign Up Today and get access to volunteer...</h3>

<%= form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>

  <%= render 'form', f: f %>

<% end %>

<%= render "devise/shared/links" %>

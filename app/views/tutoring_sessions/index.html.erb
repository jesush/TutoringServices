<h1>Scheduled Tutoring Sessions</h1>
<h4 class="subheader">View all the current tutoring sessions below. Edit, Destroy, or modify.</h4>
<p>At the moment this software is in its Beta stages. Please dont' break it. :)(Dennis....)</p>
<% if !current_user.nil? %>
<%= link_to 'Add New Tutoring Session', new_tutoring_session_path, class: "radius success button" %>
<% end %>
<table>
  <tr>
    <th>Course ID</th>
    <th>Course Name</th>
    <th>Date</th>
    <th>Start time</th>
    <th>End time</th>
    <th>Description</th>
    <th>Tutor ID</th>
    <th>Tutor Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @tutoring_sessions.each do |tutoring_session| %>
  <tr>
    <td><%= tutoring_session.course_id %></td>
    <td><%= Course.find(tutoring_session.course_id).name %></td>

    <td><%= tutoring_session.date.strftime("%A %B %e, %G") %></td>
    <td><%= tutoring_session.start_time.strftime("%I:%M%p") %></td>
    <td><%= tutoring_session.end_time.strftime("%I:%M%p")  %></td>
    
    <td><%= tutoring_session.description %></td>
    <td><%= tutoring_session.user_id %></td>
    <td><%= "#{User.find(tutoring_session.user_id).first_name if !tutoring_session.user_id.nil?} #{User.find(tutoring_session.user_id).last_name if !tutoring_session.user_id.nil?}" %></td>

    
    <td><%= link_to 'Show', tutoring_session %></td>
    <td><%= link_to 'Edit', edit_tutoring_session_path(tutoring_session) %></td>
    <td><%= link_to 'Destroy', tutoring_session, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>
